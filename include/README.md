note to anyone using this header: 

it is not always updated to the latest build. to build one for yourself, just compile
`eucalyptus-core` to generate the header. 


The header must stay here so the documentation builder dokka is able to create 
the package. 