package com.dropbear

internal actual fun EntityRef.Companion.getEntityLabel(entity: EntityId): String {
    TODO("Not yet implemented")
}

internal actual fun EntityRef.getChildren(entityId: EntityId): Array<EntityRef>? {
    TODO("Not yet implemented")
}

internal actual fun EntityRef.getChildByLabel(
    entityId: EntityId,
    label: String
): EntityRef? {
    TODO("Not yet implemented")
}

internal actual fun EntityRef.getParent(entityId: EntityId): EntityRef? {
    TODO("Not yet implemented")
}